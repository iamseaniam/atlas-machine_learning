#!/usr/bin/env python3
"""documentation"""
import gymnasium as gym

# ! is it just making an enviroment?
# ! all i am finding is stuff to do with the agent. 
# CONFUSEd
def load_frozen_lake(desc=None, map_name=None, is_slippery=False):
    """_summary_

    Args:
        desc (_type_, optional): _description_. Defaults to None.
        map_name (_type_, optional): _description_. Defaults to None.
        is_slippery (bool, optional): _description_. Defaults to False.
    """

    env = gym.make('FrozenLakeEnv')

    ):
        self.sise =